services:
  emby-users-panel:
    build: .
    image: emby-users-panel:latest
    container_name: embyuserspanel
    restart: always
    ports:
      - "8086:8086"
      - "8085:8085"
    volumes:
      - ./data:/data
    environment:
      - TZ=Asia/Shanghai
      - APP_DATA_DIR=/data
